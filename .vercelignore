# Exclude desktop source code from Vercel build
# This prevents Vercel from trying to install desktop dependencies
# or process electron-specific code.
desktop/

# Keep build artifacts if they were ever committed (though they shouldn't be)
**/node_modules
**/.next
**/dist
**/installer
**/win-unpacked
**/out
**/build_temp
